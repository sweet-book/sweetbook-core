name: sweetbook_core
description: "SweetBook photobook core: domain models (Photo, Template, PhotoBook, etc.) plus the PhotoBookPageView preview renderer widget. Web, mobile, and desktop compatible."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
